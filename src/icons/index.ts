// const req = require.context('./icons/svg', false, /\.svg$/)
// const requireAll = requireContent => requireContent.keys().map(requireContent)
// requireAll(req)

// const modules = import.meta.globEager('/src/icons/svg/*.svg')
// const requireAll = requireContent => requireContent.keys().map(requireContent)

// console.log(modules.keys, Object.keys(modules), requireAll(modules))
